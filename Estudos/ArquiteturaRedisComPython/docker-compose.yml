services:
  redis:
    image: redis:alpine
    container_name: redis-service
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      # Isso garante que o Redis não perca os dados ao reiniciar
      - ./data:/data
    command: redis-server --appendonly yes